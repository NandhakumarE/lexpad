import { useState } from "react";
import FlexContainer from "../components/FlexContainer";
import IconButton from "../components/IconButton";

const List = () => {
    const [] = useState();
    
    const onClick = () => {

    }

    return (
        <FlexContainer>
            <IconButton label="Ordered List" active={} onClick={}>
 
            </IconButton>
        </FlexContainer>
    )
}

export default List;